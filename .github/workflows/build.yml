#==========================================================================
# 用途：基于 Debian 官方 rootfs 构建 panther-x2 (RK3566) 镜像
# rebuild 工具链已内置仓库，完全独立，不依赖外部 ophub 仓库
#==========================================================================

name: Build Debian for panther-x2 (RK3566)

on:
  workflow_dispatch:
    inputs:

      debian_release:
        description: "Debian 版本"
        required: false
        default: "bookworm"
        type: choice
        options:
          - bookworm
          - trixie

      armbian_kernel:
        description: "内核版本"
        required: false
        default: "6.12.y"
        type: choice
        options:
          - 6.12.y
          - 6.6.y
          - 6.1.y

      auto_kernel:
        description: "自动使用同系列最新内核"
        required: false
        default: true
        type: boolean

      armbian_fstype:
        description: "rootfs 文件系统类型"
        required: false
        default: "ext4"
        type: choice
        options:
          - ext4
          - btrfs

      root_password:
        description: "root 密码"
        required: false
        default: "1234"
        type: string

      hostname:
        description: "主机名"
        required: false
        default: "debian"
        type: string

      timezone:
        description: "时区"
        required: false
        default: "Asia/Shanghai"
        type: choice
        options:
          - Asia/Shanghai
          - Asia/Tokyo
          - America/New_York
          - Europe/London
          - UTC

      rootfs_url:
        description: "复用已有 rootfs tar.gz（填写 URL 则跳过 debootstrap）"
        required: false
        default: ""
        type: string

      builder_name:
        description: "构建者签名"
        required: false
        default: "mybuilder"
        type: string

env:
  TZ: UTC
  DEBIAN_MIRROR: "https://deb.debian.org/debian"
  ROOTFS_DIR: /builder/debian-rootfs
  BOARD: panther-x2

jobs:
  build:
    runs-on: ubuntu-24.04
    if: ${{ github.event.repository.owner.id == github.event.sender.id }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # ── 1. 初始化环境 ──────────────────────────────────────────────────
      - name: Initialize environment
        id: init
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          docker rmi -f $(docker images -q) 2>/dev/null || true
          [[ -n "${AGENT_TOOLSDIRECTORY}" ]] && sudo rm -rf "${AGENT_TOOLSDIRECTORY}"
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android 2>/dev/null
          sudo swapoff -a && sudo rm -f /swapfile /mnt/swapfile
          sudo -E apt-get -y update
          sudo -E apt-get -y purge \
            azure-cli ghc* zulu* llvm* firefox google* dotnet* \
            powershell openjdk* mongodb* moby* 2>/dev/null || true
          sudo -E apt-get -y install \
            debootstrap qemu-user-static binfmt-support \
            parted e2fsprogs dosfstools btrfs-progs \
            u-boot-tools uuid-runtime openssl pigz \
            git curl wget xz-utils lz4 zstd \
            pv bc jq
          sudo -E apt-get -y autoremove --purge
          sudo -E apt-get clean
          sudo -E timedatectl set-timezone "${TZ}"
          echo "status=success" >> "${GITHUB_OUTPUT}"

      # ── 2. 合并磁盘空间 ────────────────────────────────────────────────
      - name: Create build disk
        if: ${{ steps.init.outputs.status == 'success' && !cancelled() }}
        run: |
          mnt_size=$(expr $(df -h /mnt | tail -1 | awk '{print $4}' | sed 's/[[:alpha:]]//g' | sed 's/\\..*//') - 1)
          root_size=$(expr $(df -h / | tail -1 | awk '{print $4}' | sed 's/[[:alpha:]]//g' | sed 's/\\..*//') - 4)
          sudo truncate -s "${mnt_size}G" /mnt/mnt.img
          sudo truncate -s "${root_size}G" /root.img
          sudo losetup /dev/loop6 /mnt/mnt.img
          sudo losetup /dev/loop7 /root.img
          sudo pvcreate /dev/loop6 /dev/loop7
          sudo vgcreate github /dev/loop6 /dev/loop7
          sudo lvcreate -n runner -l 100%FREE github
          sudo mkfs.xfs -f -i sparse=0 -b size=4096 /dev/github/runner
          sudo mkdir -p /builder
          sudo mount /dev/github/runner /builder
          sudo chown -R runner:runner /builder
          echo "可用空间: $(df -h /builder | tail -1 | awk '{print $4}')"

      # ── 3. 构建 Debian rootfs ──────────────────────────────────────────
      - name: Build Debian rootfs
        id: rootfs
        if: ${{ steps.init.outputs.status == 'success' && !cancelled() }}
        env:
          DEBIAN_RELEASE: ${{ inputs.debian_release }}
          ROOT_PASSWORD:  ${{ inputs.root_password }}
          HOSTNAME_VAL:   ${{ inputs.hostname }}
          TZ_VAL:         ${{ inputs.timezone }}
          ROOTFS_URL:     ${{ inputs.rootfs_url }}
        run: |
          set -euo pipefail
          sudo mkdir -p "${ROOTFS_DIR}"

          if [[ -n "${ROOTFS_URL}" ]]; then
            echo "▶ 使用已有 rootfs：${ROOTFS_URL}"
            ROOTFS_ARCHIVE="/tmp/rootfs_input.tar.gz"
            wget -q --show-progress -O "${ROOTFS_ARCHIVE}" "${ROOTFS_URL}" \
              || { echo "::error::rootfs 下载失败"; exit 1; }
            sudo tar -xf "${ROOTFS_ARCHIVE}" -C "${ROOTFS_DIR}" \
              || { echo "::error::rootfs 解压失败"; exit 1; }
            rm -f "${ROOTFS_ARCHIVE}"
            for _dir in etc usr bin; do
              [[ -d "${ROOTFS_DIR}/${_dir}" ]] || { echo "::error::rootfs 缺少 /${_dir}"; exit 1; }
            done
            echo "▶ rootfs 校验通过，跳过 debootstrap"
          else
            sudo update-binfmts --enable qemu-aarch64
            echo "▶ debootstrap 第一阶段 (${DEBIAN_RELEASE})..."
            sudo debootstrap \
              --arch=arm64 --foreign --include=ca-certificates \
              "${DEBIAN_RELEASE}" "${ROOTFS_DIR}" "${DEBIAN_MIRROR}/"
            sudo cp /usr/bin/qemu-aarch64-static "${ROOTFS_DIR}/usr/bin/"
            echo "▶ debootstrap 第二阶段..."
            sudo chroot "${ROOTFS_DIR}" /debootstrap/debootstrap --second-stage
          fi

          # ── apt 源 ──────────────────────────────────────────────────────
          printf 'deb %s/ %s main contrib non-free non-free-firmware\n' \
            "${DEBIAN_MIRROR}" "${DEBIAN_RELEASE}" \
            | sudo tee    "${ROOTFS_DIR}/etc/apt/sources.list" > /dev/null
          printf 'deb %s/ %s-updates main contrib non-free non-free-firmware\n' \
            "${DEBIAN_MIRROR}" "${DEBIAN_RELEASE}" \
            | sudo tee -a "${ROOTFS_DIR}/etc/apt/sources.list" > /dev/null
          printf 'deb https://security.debian.org/debian-security %s-security main contrib non-free non-free-firmware\n' \
            "${DEBIAN_RELEASE}" \
            | sudo tee -a "${ROOTFS_DIR}/etc/apt/sources.list" > /dev/null

          # ── 安装基础包 ──────────────────────────────────────────────────
          echo "▶ 安装基础软件包..."
          sudo chroot "${ROOTFS_DIR}" /bin/bash -c "
            export DEBIAN_FRONTEND=noninteractive
            apt-get update -qq
            apt-get install -y \
              openssh-server sudo curl wget ca-certificates \
              locales tzdata \
              iproute2 network-manager ifupdown \
              net-tools iputils-ping dnsutils \
              wireless-tools chrony \
              bash-completion htop nano vim nftables \
              u-boot-tools initramfs-tools parted \
              btrfs-progs e2fsprogs dosfstools \
              lsof file procps cron ntpdate bsdextrautils \
              wireless-regdb \
              --no-install-recommends
            # wpasupplicant 单独安装确保 libnl 依赖完整
            apt-get install -y wpasupplicant
            apt-get clean && rm -rf /var/lib/apt/lists/*
          "

          # ── 额外包（可选）───────────────────────────────────────────────
          EXTRA_PKGS="${{ inputs.extra_packages || '' }}"
          if [[ -n "${EXTRA_PKGS}" ]]; then
            echo "▶ 安装额外包: ${EXTRA_PKGS}"
            sudo chroot "${ROOTFS_DIR}" /bin/bash -c "
              export DEBIAN_FRONTEND=noninteractive
              apt-get update -qq
              apt-get install -y ${EXTRA_PKGS} --no-install-recommends
              apt-get clean && rm -rf /var/lib/apt/lists/*
            "
          fi

          # ── 时区 ────────────────────────────────────────────────────────
          sudo chroot "${ROOTFS_DIR}" ln -sf "/usr/share/zoneinfo/${TZ_VAL}" /etc/localtime
          echo "${TZ_VAL}" | sudo tee "${ROOTFS_DIR}/etc/timezone" > /dev/null
          sudo chroot "${ROOTFS_DIR}" bash -c \
            "DEBIAN_FRONTEND=noninteractive dpkg-reconfigure -f noninteractive tzdata"

          # ── locale ──────────────────────────────────────────────────────
          sudo chroot "${ROOTFS_DIR}" bash -c "
            sed -i 's/^# *\(en_US.UTF-8\)/\1/' /etc/locale.gen
            locale-gen
            echo 'LANG=en_US.UTF-8' > /etc/default/locale
          "

          # ── hostname ────────────────────────────────────────────────────
          echo "${HOSTNAME_VAL}" | sudo tee "${ROOTFS_DIR}/etc/hostname" > /dev/null
          printf '127.0.0.1\tlocalhost\n127.0.1.1\t%s\n::1\tlocalhost ip6-localhost ip6-loopback\n' \
            "${HOSTNAME_VAL}" | sudo tee "${ROOTFS_DIR}/etc/hosts" > /dev/null

          # ── SSH ─────────────────────────────────────────────────────────
          sudo mkdir -p -m 0755 "${ROOTFS_DIR}/var/run/sshd"
          echo 'UG9ydCAyMgpQZXJtaXRSb290TG9naW4geWVzClBhc3N3b3JkQXV0aGVudGljYXRpb24geWVzClB1YmtleUF1dGhlbnRpY2F0aW9uIHllcwpVc2VQQU0geWVzClgxMUZvcndhcmRpbmcgbm8KUHJpbnRNb3RkIG5vCkFjY2VwdEVudiBMQU5HIExDXyoKU3Vic3lzdGVtIHNmdHAgL3Vzci9saWIvb3BlbnNzaC9zZnRwLXNlcnZlcgo=' \
            | base64 -d | sudo tee "${ROOTFS_DIR}/etc/ssh/sshd_config" > /dev/null
          sudo chroot "${ROOTFS_DIR}" systemctl enable ssh 2>/dev/null || true
          sudo chroot "${ROOTFS_DIR}" systemctl enable chrony 2>/dev/null || true
          sudo chroot "${ROOTFS_DIR}" systemctl enable nftables 2>/dev/null || true

          # ── root 密码 ───────────────────────────────────────────────────
          ROOT_HASH="$(openssl passwd -6 "${ROOT_PASSWORD}")"
          sudo sed -i "s|^root:[^:]*|root:${ROOT_HASH}|" "${ROOTFS_DIR}/etc/shadow"

          # ── NetworkManager ──────────────────────────────────────────────
          sudo mkdir -p "${ROOTFS_DIR}/etc/NetworkManager"
          echo 'W21haW5dCnBsdWdpbnM9aWZ1cGRvd24sa2V5ZmlsZQpkaGNwPWludGVybmFsCltpZnVwZG93bl0KbWFuYWdlZD10cnVlCg==' \
            | base64 -d | sudo tee "${ROOTFS_DIR}/etc/NetworkManager/NetworkManager.conf" > /dev/null
          printf 'auto lo\niface lo inet loopback\n' \
            | sudo tee "${ROOTFS_DIR}/etc/network/interfaces" > /dev/null
          sudo chroot "${ROOTFS_DIR}" systemctl enable NetworkManager 2>/dev/null || true

          # ── 禁用不必要的服务 ────────────────────────────────────────────
          sudo chroot "${ROOTFS_DIR}" systemctl disable networking.service 2>/dev/null || true
          sudo chroot "${ROOTFS_DIR}" systemctl disable NetworkManager-wait-online.service 2>/dev/null || true
          sudo chroot "${ROOTFS_DIR}" systemctl disable e2scrub_reap.service 2>/dev/null || true
          sudo chroot "${ROOTFS_DIR}" systemctl disable systemd-pstore.service 2>/dev/null || true

          # ── systemd 超时优化 ────────────────────────────────────────────
          SYS_CONF="${ROOTFS_DIR}/etc/systemd/system.conf"
          [[ -f "${SYS_CONF}" ]] && {
            sudo sed -i "s|^#*DefaultTimeoutStartSec.*|DefaultTimeoutStartSec=10s|g" "${SYS_CONF}"
            sudo sed -i "s|^#*DefaultTimeoutStopSec.*|DefaultTimeoutStopSec=10s|g"   "${SYS_CONF}"
          } || true

          # ── rc.local ────────────────────────────────────────────────────
          if [[ ! -f "${ROOTFS_DIR}/etc/rc.local" ]]; then
            printf '#!/bin/sh -e\nexit 0\n' | sudo tee "${ROOTFS_DIR}/etc/rc.local" > /dev/null
            sudo chmod +x "${ROOTFS_DIR}/etc/rc.local"
          fi

          # ── 注入 debian 平台脚本 ────────────────────────────────────────
          sudo mkdir -p "${ROOTFS_DIR}/usr/lib/debian"
          sudo mkdir -p "${ROOTFS_DIR}/etc/default"
          sudo mkdir -p "${ROOTFS_DIR}/lib/systemd/system"

          for f in debian/debian-firstrun \
                   debian/debian-hardware-optimization \
                   debian/debian-resize-filesystem \
                   debian/debian-zram-config; do
            sudo cp "${f}" "${ROOTFS_DIR}/usr/lib/debian/$(basename ${f})"
          done
          sudo chmod +x "${ROOTFS_DIR}/usr/lib/debian/"*

          sudo cp debian/debian-release       "${ROOTFS_DIR}/etc/debian-release"
          sudo cp debian/debian-firstrun-etc  "${ROOTFS_DIR}/etc/default/debian-firstrun"
          sudo cp debian/NetworkManager.conf  "${ROOTFS_DIR}/etc/NetworkManager/NetworkManager.conf"
          sudo cp debian/debian-update        "${ROOTFS_DIR}/usr/local/bin/debian-update"
          sudo chmod +x "${ROOTFS_DIR}/usr/local/bin/debian-update"

          # ── systemd service 文件 ────────────────────────────────────────
          for f in systemd/debian-firstrun.service \
                   systemd/debian-hardware-optimize.service \
                   systemd/debian-resize-filesystem.service \
                   systemd/debian-zram-config.service \
                   systemd/plymouth-killer.service; do
            sudo cp "${f}" "${ROOTFS_DIR}/lib/systemd/system/"
          done

          sudo chroot "${ROOTFS_DIR}" systemctl enable debian-firstrun.service
          sudo chroot "${ROOTFS_DIR}" systemctl enable debian-hardware-optimize.service
          sudo chroot "${ROOTFS_DIR}" systemctl enable debian-resize-filesystem.service
          sudo chroot "${ROOTFS_DIR}" systemctl enable debian-zram-config.service
          sudo chroot "${ROOTFS_DIR}" systemctl enable plymouth-killer.service

          # ── 禁用 plymouth initramfs hook ────────────────────────────────
          sudo mkdir -p "${ROOTFS_DIR}/usr/share/initramfs-tools/hooks"
          sudo ln -sf /dev/null "${ROOTFS_DIR}/usr/share/initramfs-tools/hooks/plymouth"

          # ── uInitrd 钩子 ────────────────────────────────────────────────
          sudo mkdir -p "${ROOTFS_DIR}/etc/initramfs/post-update.d"
          echo '' \
            | base64 -d | sudo tee "${ROOTFS_DIR}/etc/initramfs/post-update.d/99-uboot" > /dev/null
          sudo chmod +x "${ROOTFS_DIR}/etc/initramfs/post-update.d/99-uboot"

          # ── 禁止 update-initramfs 自动运行 ──────────────────────────────
          sudo mkdir -p "${ROOTFS_DIR}/etc/initramfs-tools"
          INITRAMFS_CONF="${ROOTFS_DIR}/etc/initramfs-tools/update-initramfs.conf"
          if [[ -f "${INITRAMFS_CONF}" ]]; then
            sudo sed -i "s|^update_initramfs=.*|update_initramfs=no|g" "${INITRAMFS_CONF}"
          else
            echo "update_initramfs=no" | sudo tee "${INITRAMFS_CONF}" > /dev/null
          fi

          # ── fstab ───────────────────────────────────────────────────────
          printf 'LABEL=ROOTFS\t/\text4\tdefaults,noatime,nodiratime,commit=600,errors=remount-ro\t0 1\n' \
            | sudo tee    "${ROOTFS_DIR}/etc/fstab" > /dev/null
          printf 'LABEL=BOOT\t/boot\tvfat\tdefaults\t0 2\n' \
            | sudo tee -a "${ROOTFS_DIR}/etc/fstab" > /dev/null

          sudo mkdir -p "${ROOTFS_DIR}/usr/lib/modules"
          sudo rm -f "${ROOTFS_DIR}/usr/bin/qemu-aarch64-static"

          echo "▶ rootfs 完成: $(sudo du -sh ${ROOTFS_DIR} | awk '{print $1}')"
          echo "status=success" >> "${GITHUB_OUTPUT}"

      # ── 4. 打包 rootfs 为镜像 ─────────────────────────────────────────
      - name: Pack rootfs into image
        id: pack
        if: ${{ steps.rootfs.outputs.status == 'success' && !cancelled() }}
        env:
          DEBIAN_RELEASE: ${{ inputs.debian_release }}
        run: |
          set -euo pipefail
          IMG_OUTDIR="${GITHUB_WORKSPACE}/build/output/images"
          mkdir -p "${IMG_OUTDIR}"
          IMG_NAME="Armbian_22.02.0-trunk_${DEBIAN_RELEASE}_arm64.img"
          IMG_PATH="${IMG_OUTDIR}/${IMG_NAME}"

          USED_MB=$(sudo du -sm "${ROOTFS_DIR}" | awk '{print $1}')
          IMG_MB=$(( USED_MB + 4 + 64 ))
          echo "▶ rootfs ${USED_MB} MiB → 镜像 ${IMG_MB} MiB"

          truncate -s "${IMG_MB}M" "${IMG_PATH}"
          sudo parted -s "${IMG_PATH}" mklabel msdos
          sudo parted -s "${IMG_PATH}" mkpart primary ext4 4MiB 100%

          LOOP=$(sudo losetup -P -f --show "${IMG_PATH}")
          echo "▶ loop: ${LOOP}"

          sudo mkfs.ext4 -F -q -L "ROOTFS" -b 4096 "${LOOP}p1"

          MNT="/mnt/rootfs_pack_$$"
          sudo mkdir -p "${MNT}"
          sudo mount "${LOOP}p1" "${MNT}"
          sudo cp -a "${ROOTFS_DIR}/." "${MNT}/"
          sudo sync
          sudo umount "${MNT}"
          sudo losetup -d "${LOOP}"
          sudo rmdir "${MNT}"

          echo "▶ $(du -sh ${IMG_PATH})"
          echo "img_name=${IMG_NAME}"   >> "${GITHUB_OUTPUT}"
          echo "img_path=${IMG_PATH}"   >> "${GITHUB_OUTPUT}"
          echo "img_outdir=${IMG_OUTDIR}" >> "${GITHUB_OUTPUT}"
          echo "status=success"         >> "${GITHUB_OUTPUT}"

      # ── 5. Rebuild（注入 uboot + kernel，使用本地 rebuild 工具链）─────
      - name: Rebuild image
        id: rebuild
        if: ${{ steps.pack.outputs.status == 'success' && !cancelled() }}
        env:
          KERNEL:      ${{ inputs.armbian_kernel }}
          AUTO_KERNEL: ${{ inputs.auto_kernel }}
          FSTYPE:      ${{ inputs.armbian_fstype }}
          BUILDER:     ${{ inputs.builder_name }}
        run: |
          set -euo pipefail
          REBUILD_DIR="${GITHUB_WORKSPACE}/rebuild"
          IMG_PATH="${{ steps.pack.outputs.img_path }}"
          IMG_OUTDIR="${{ steps.pack.outputs.img_outdir }}"

          # rebuild 脚本以 PWD 为工作目录，build-armbian/ 必须在同级
          cd "${REBUILD_DIR}"

          mkdir -p build/output/images
          cp -v "${IMG_PATH}" build/output/images/

          ARGS="-b ${BOARD} -a ${AUTO_KERNEL} -t ${FSTYPE}"
          ARGS="${ARGS} -s 512/3072 -r ophub/kernel -u rk35xx -n ${BUILDER}"
          if [[ "${AUTO_KERNEL}" != "true" ]]; then
            KERNEL_VER="${KERNEL##*/}"
            ARGS="${ARGS} -k ${KERNEL_VER}"
          fi

          echo "▶ 执行: sudo ./rebuild ${ARGS}"
          sudo ./rebuild ${ARGS}

          OUTPUT_DIR="${REBUILD_DIR}/build/output/images"
          ls -lh "${OUTPUT_DIR}/"

          PACKAGED_FILE="$(ls ${OUTPUT_DIR}/*.img.gz 2>/dev/null | head -1)"
          [[ -z "${PACKAGED_FILE}" ]] && { echo "::error::rebuild 未生成 .img.gz"; exit 1; }

          mv -v "${OUTPUT_DIR}"/*.img.gz* "${IMG_OUTDIR}/" 2>/dev/null || true

          echo "PACKAGED_OUTPUTPATH=${IMG_OUTDIR}" >> "${GITHUB_ENV}"
          echo "PACKAGED_STATUS=success"           >> "${GITHUB_ENV}"
          echo "status=success"                    >> "${GITHUB_OUTPUT}"
          ls -lh "${IMG_OUTDIR}/"

      # ── 6. 发布到 Release ─────────────────────────────────────────────
      - name: Upload image to Release
        uses: ncipollo/release-action@main
        if: ${{ env.PACKAGED_STATUS == 'success' && !cancelled() }}
        with:
          tag: "Debian_${{ inputs.debian_release }}_panther-x2_${{ inputs.armbian_kernel }}_${{ inputs.armbian_fstype }}"
          name: "Debian ${{ inputs.debian_release }} · panther-x2 · ${{ inputs.armbian_kernel }}"
          artifacts: "${{ env.PACKAGED_OUTPUTPATH }}/*"
          allowUpdates: true
          removeArtifacts: false
          replacesArtifacts: true
          makeLatest: true
          token: ${{ secrets.GITHUB_TOKEN }}
          body: |
            ## Debian ${{ inputs.debian_release }} for panther-x2 (RK3566)

            | 参数 | 值 |
            |------|-----|
            | Debian 版本 | `${{ inputs.debian_release }}` |
            | 内核 | `${{ inputs.armbian_kernel }}` |
            | 文件系统 | `${{ inputs.armbian_fstype }}` |
            | 构建时间 | `${{ github.run_started_at }}` |

            ## 使用说明

            1. 下载 `.img.gz`，解压得到 `.img`
            2. 用 balenaEtcher 或 `dd` 写入 TF 卡 / U 盘
            3. SSH：`root` / `${{ inputs.root_password }}`
            4. 安装到 eMMC：`armbian-install`
            5. 更新内核：`debian-update`
